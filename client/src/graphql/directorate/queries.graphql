query DirectorateById($id: ObjectId!) {
    DirectorateById(id: $id) {
        _id
        title
    }
}

query DirectorateMany {
    DirectorateMany {
        _id
        title
        updatedAt
        updatedBy {
            _id
            firstName
            lastName
        }
    }
}
